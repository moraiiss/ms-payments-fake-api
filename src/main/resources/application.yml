server:
  port: 8081

spring:
  application:
    name: ms-payments-fake-api

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: luiz.morais
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
